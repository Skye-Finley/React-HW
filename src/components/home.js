import React,{Component} from 'react';
export class Home extends Component{
    render(){
        return(
            <div className="mt-5 d-flex justify-content-left">
            <h3>Welcome Employee Management Portal.
                This is the home page.</h3>
            </div>
        )
    }
}

var numbers = [32, 33, 17, 41];

function checkNumber(Odd) {
  return number >= Odd;
}

function myFunction() {
  document.getElementById("demo").innerHTML = number.filter(checkOdd);
}